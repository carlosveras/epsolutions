@model IEnumerable<EPSolutions.Models.ItensRomaneio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NrCaixa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescrVolumes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entregue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdRomaneioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NrCaixa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescrVolumes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Entregue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdRomaneioNavigation.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
